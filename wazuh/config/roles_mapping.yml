---
_meta:
  type: "rolesmapping"
  config_version: 2

all_access:
  reserved: false
  backend_roles:
    - "admin"
  description: "Maps admin to all_access"

own_index:
  reserved: false
  users: []
  description: "Disabled"

kibana_user:
  reserved: false
  backend_roles:
    - "collaborator"
    - "manager"
    - "rh"
    - "direction"

readall:
  reserved: false
  backend_roles:
    - "collaborator"

kibana_server:
  reserved: true
  users:
    - "kibanaserver"
